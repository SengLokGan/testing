export enum Answer {
  Yes = 'YES',
  No = 'No',
}

export enum HdPrescriptionStatuses {
  ACTIVE = 'ACTIVE',
  DISCONTINUED = 'DISCONTINUED',
}

export enum AccessManagementStatuses {
  ACTIVE = 'ACTIVE',
  DISCONTINUED = 'DISCONTINUED',
}

export enum hdPrescriptionFrequencyCodes {
  ONCE_PER_WEEK = 'Once per week',
  TWICE_PER_WEEK = 'Twice per week',
  THRICE_PER_WEEK = 'Thrice per week',
  DAILY = 'Daily',
}

export enum MedicationDrawerType {
  Add = 'ADD_MEDICATION',
  Edit = 'EDIT_MEDICATION',
  Confirm = 'CONFIRM_MEDICATION',
  Change = 'CHANGE_MEDICATION',
}
