export enum AppointmentsStatusesFilters {
  All = 'ALL',
  Waitlist = 'WAIT_LIST',
  InProgress = 'IN_PROGRESS',
  Complete = 'COMPLETED',
  Cancelled = 'CANCELLED',
}

export enum TodayPatientsTabs {
  Patients = 'Patients',
  Injections = 'Injections',
}

export enum TodayPatientsViewMode {
  Table = 'TABLE_VIEW',
  Grid = 'GRID_VIEW',
}
