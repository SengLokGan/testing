/// <reference types="react" />
import { WithSx } from '@types';
interface GlobalLoaderProps extends WithSx {
  invisible?: boolean;
}
export declare const GlobalLoader: ({ invisible, sx }: GlobalLoaderProps) => JSX.Element;
export {};
