/// <reference types="react" />
import { AppointmentsGroup } from '@types';
type AppointmentGroupProps = {
  group: AppointmentsGroup;
};
export declare const AppointmentGroup: ({ group }: AppointmentGroupProps) => JSX.Element;
export {};
