/// <reference types="react" />
import { CustomEvent } from '@types';
type CustomEventCardProps = {
  event: CustomEvent;
};
export declare const CustomEventCard: ({ event }: CustomEventCardProps) => JSX.Element;
export {};
