/// <reference types="react" />
import { Pagination } from '@types';
type PatientsGridProps = {
  pagination: Pagination;
};
export declare const PatientsGrid: ({ pagination }: PatientsGridProps) => JSX.Element;
export {};
